apiVersion: apps/v1  
kind: Deployment  
metadata:  
  name: fastapi-service  
spec:  
  replicas: 1  
  selector:  
    matchLabels:  
      app: fastapi-service
  template:  
    metadata:  
      labels:  
        app: fastapi-service  
    spec:  
      containers:  
        - name: fastapi-service  
          image: ajh9789/fastapi_service:latest  
          ports:  
            - containerPort: 8000  
          env:  
            - name: REDIS_HOST  
              value: redis

---  
apiVersion: v1  
kind: Service  
metadata:  
  name: fastapi-service  
spec:  
  type: NodePort  
  selector:  
    app: fastapi-service
  ports:  
    - port: 8000  
      targetPort: 8000  
      nodePort: 30080
